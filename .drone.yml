build:
  image: ubuntu:trusty
  commands:
    - echo 'OK'

publish:
  docker:
    registry: registry.40ants.com:5000
    repo: allmychanges.com
    tag: new
    insecure: true
    when:
      branch: master
